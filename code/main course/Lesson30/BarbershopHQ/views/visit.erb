<h2>Записаться</h2>
<p>На этой странице Вы можете записаться к нам в Barber Shop</p>
<form action="/visit" method="POST">
  <div class="form-group">
    <label for="exampleInputName1">Ваше имя</label>
    <input
      name="client[name]"
      type="text"
      class="form-control"
      id="exampleInputName1"
      placeholder="Введите Ваше имя"
      value="<%= @c.name %>"
    />
  </div>
  <div class="form-group">
    <label for="exampleInputPhone2">Ваш телефон</label>
    <input
      name="client[phone]"
      type="phote"
      class="form-control"
      id="exampleInputPhone2"
      placeholder="Введите Ваш телефон"
      value="<%= @c.phone %>"
    />
  </div>
  <div class="form-group">
    <label for="exampleInputDateTime3">Когда Вас ждать?</label>
    <input
      id="datetimepicker"
      name="client[datestamp]"
      type="text"
      class="form-control"
      id="exampleInputDateTime3"
      placeholder="Введите дату и время"
      value="<%= @c.datestamp %>"
    />
  </div>
  <div class="form-group">
    <label for="choose_hairdresser">Выбор парикмахера</label>
    <select 
    class="form-control" 
    name="client[barber]"
    >
      <% @barbers.each do |item| %>
      <option <%= @choose_hairdresser == item['name'] ? 'selected' : '' %>><%= item['name']%></option>
      <% end %>
    </select>
  </div>
  <div class="form-group">
    <label for="colorpicker">В какие цвета хотите себя окрасить:</label>
    <select id="color" name="client[color]">
      <option value="#7bd148">Green</option>
      <option value="#5484ed">Bold blue</option>
      <option value="#a4bdfc">Blue</option>
      <option value="#46d6db">Turquoise</option>
      <option value="#7ae7bf">Light green</option>
      <option value="#51b749">Bold green</option>
      <option value="#fbd75b">Yellow</option>
      <option value="#ffb878">Orange</option>
      <option value="#ff887c">Red</option>
      <option value="#dc2127">Bold red</option>
      <option value="#dbadff">Purple</option>
      <option value="#e1e1e1">Gray</option>
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Записаться</button>
</form>

<script type="text/javascript">
  $(function() {
    $('#color').simplecolorpicker();
    $('#datetimepicker').datetimepicker();
  });
</script>
